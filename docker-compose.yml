version: '3.7'

services:
  runner: 
    image: node
    ports: 
      - "$PORT:3000"
      - "9229:9229"
    volumes:
    - .:/app:cached
    - yarn-cache-volume:/usr/local/share/.cache/yarn/v6:cached
    working_dir: /app
    command: "$COMMAND"
  
volumes:
  yarn-cache-volume: